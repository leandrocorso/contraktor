import React from 'react';
// Material UI
import MyInputLabel from '@material-ui/core/InputLabel';

function MyMyInputLabel(props) {
  return <MyInputLabel {...props} />;
}

export default MyMyInputLabel;
